# JLPT N2 Training Application

Приложение для подготовки к экзамену JLPT N2 (Nihongo Noryoku Shiken).
Включает в себя тесты на лексику, грамматику и чтение.

## 🚀 Функциональность

- **Квизы**: Прохождение тестов по разным главам и типам вопросов.
- **Админ-панель**: Управление вопросами, главами и текстами для чтения.
- **Импорт**: Массовая загрузка вопросов из JSON файлов.
- **Поддержка разметки**: Автоматическое подчеркивание текста, выделенного `__двойным подчеркиванием__`.

## 🛠️ Установка и запуск

### 1. Предварительные требования

- Node.js (версия 16 или выше)
- npm

### 2. Установка зависимостей

```bash
npm install
```

### 3. Инициализация базы данных

Эта команда создаст базу данных SQLite и заполнит её начальными данными (типы вопросов, примеры).
**Внимание:** Это удалит все существующие данные!

```bash
npm run seed
```

### 4. Запуск приложения

Запускает и сервер (backend), и клиент (frontend) одной командой.

```bash
npm run dev:all
```

Приложение будет доступно по адресу: `http://localhost:5173`

---

## 📚 Импорт вопросов

Вы можете массово импортировать вопросы из JSON файлов.
Формат файла должен соответствовать структуре, которую генерирует AI (см. `docs/HOW_TO_ADD_QUESTIONS.md`).

### Команда импорта:

```bash
node server/import_json.js <путь_к_файлу.json>
```

Пример:

```bash
node server/import_json.js chapter1.json
```

Скрипт автоматически:

- Создаст главу, если её нет.
- Определит тип вопроса.
- Добавит вопросы и ответы в базу данных.
- Поддерживает вопросы к текстам (Mondai 9).

---

## ⚙️ Админ-панель

Админ-панель доступна по адресу: `/admin` (например, `http://localhost:5173/admin`).

Возможности:

- **Фильтрация**: Просмотр вопросов по главам и типам.
- **Редактирование**: Изменение текста вопроса, вариантов ответов и правильного ответа.
- **Mondai 8**: Специальный интерфейс для вопросов на порядок слов (выбор позиции звезды).

## 📝 Формат вопросов

Для выделения ключевого слова (подчеркивания) в вопросе используйте двойное нижнее подчеркивание:

```json
"content": "彼女は子どものとき、__行儀作法__を家庭で厳しく教え込まれた。"
```

В приложении это отобразится как: 彼女は子どものとき、<u>行儀作法</u>を家庭で厳しく教え込まれた。
